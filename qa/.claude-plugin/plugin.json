{
  "name": "qa",
  "description": "Manual QA runs driven through a browser — derive the scenarios from the ticket and the diff, write them down as a scenario file under docs/qa/<TASK>/, execute them with Playwright in a QA window that stays open between runs (Chromium or Brave, own profile) or through the user's own Chrome, caption every screenshot with what it proves, and report pass/fail with reproduction steps",
  "version": "0.3.0",
  "author": {
    "name": "mrstroz"
  }
}
